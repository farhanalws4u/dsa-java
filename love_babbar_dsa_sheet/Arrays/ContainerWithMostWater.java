package love_babbar_dsa_sheet.Arrays;

public class ContainerWithMostWater {
    // int left = 0;
    // int right = height.length - 1;
    // int length = 0;
    // int currentArea = 0;
    // int finalArea = 0;

    // while(left<right)
    // {

    // length = Math.min(height[right], height[left]);

    // currentArea = Math.abs((right - left) * length);

    // if (currentArea > finalArea) {
    // finalArea = currentArea;
    // }

    // if (height[left] < height[right]) {
    // left++;
    // } else {
    // right--;
    // }

    // }

    // return finalArea;
}
